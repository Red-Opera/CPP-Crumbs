#pragma warning(disable:4996)
#include <stdio.h>

int main()
{
	float a = 0, b = 0, x = 0, z = 0, y = 0, t = 0,fly = 0;  // 기차위치 a,b 이동속도 x,z,y, 케이스 개수 초기화

	scanf("%f", &t);
	scanf("%f %f", &a, &b);
	scanf("%f %f %f", &x, &y, &z);

	while ((t >= 1) && (t <= 10))
	{
		if (a == 0)
		{
			printf("1");
			printf("\n");
		}
		else if (b == 0)
		{
			printf("2");
			printf("\n");
		}
		else
		{
			printf("3");
			printf("\n");
		}

		if (z < x)
		{
			while
		}
		else if (y < z)
		{
			
		}
		return 0;
	}
}