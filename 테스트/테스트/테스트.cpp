#pragma warning(disable:4996)
#include <stdlib.h>
#include <stdio.h>

int main(void)
{
	int a;
	int c[999];

	scanf ("%d", &a);
	c[a];
	for (int b = a; b >= 0; b--) {


		printf("a의값은 %d 입니다.\n", b);
	}
	system("pause");
}